# def Ramesh_SureshAreGoodFriends(BothLikeToEat5Star, BothWearSunGlasses):
#     ButNowRameshLikesDairyMilk = [SureshLikesKitKat]
#     for BothArgueOverChocolates, MeanwhileTheirChocolateStartMelting not in WhichChocolateisBetter(TheOneWhichIsMoreChocolaty):
#         WhichisMoreChocolaty = KitKat + DairyMilk
#         for BetterChocolate in range(Taste):
#             print([TheSweeter[TheBetter], Chocolate]
#         TheyDecideToEat[BothOfThemTogether] = ButTheyBoth
#     print ["Abandon 5 Star]
# NowNoOne = dict(map(input("Now No One Likes 5 Star").rstrip()))
# RameshandSuresh =(Together("LiveHappilyEverAfter"))
# EatingBoth = Dairy_Milk(and, 5Star)

#chocolatey fight
# def WhichChocolateisBetter(Chocolate, target):
#     for i in range(len(Chocolate)):
#         for j in range(i + 1, len(Chocolate)):
#             if Chocolate[i] + Chocolate[j] == target:
#                 print(f"Indices: {i} and {j}")
#                 print(f"Numbers: {Chocolate[i]} and {Chocolate[j]}")

# n = [int(x) for x in input().split()]
# target = int(input())
# WhichChocolateisBetter(n, target)


#ashichooseyou
# class PikachuIchOOseYoU:
#     def __init__(self, bolt):
#         self.bolt = bolt 

#     def water(self):
#         choose = len(str(self.bolt))
#         fav = self.bolt
#         allplmao = 0
#         while fav > 0:
#             digit = fav % 10
#             allplmao += digit ** choose
#             fav //= 10
#         return self.bolt == allplmao

# very = int(input("very"))
# strong = PikachuIchOOseYoU(very)
# if strong.water():
#     print("True")
# else:
#     print("False")

# it’s equal to the sum of its own potential, raised by what defines it.”
#when a being’s power equals the sum of its own parts, each raised to its nature.
#“It’s the one whose power perfectly reflects who they are
#Sample Input 0

# 123

# Sample Output 0

# False

def is_validPassword(TempleGate):
    if SacredNum > len(pasS):
        return "Password is Correct"
    elif NumberOfPeople >= 3:
         print("Each Person has to Donate 100Rs")
    if num / 2 == 0:
        print("The gate will be locked if another wrong password is entered")
    if i in range(len(temple), string(FirstDigit ** lastDigit) , len(DoorofTemple)):
        if Trials % IncorrectAttempts == 0:
            return "Temple Locked Forever"
    return "Gate Opened Pray Well"
def Guesses(Probability):
    Guess_valid = [Prob[CorrectChar]]
    Tukka1, Tukka2 = Donation1, Donation2
    for i in range(len(hint_numbers) < Password_len):
        Pass1, Pass2 = Donation1+Donation2+Donation3
        if password_valid(CombinedTukka):
            AncientPassword(Opened)
    return "GateOpenedOrNot"
n = int(input())
CanweEnter = CanweEnterTheTemple(Yes/No)
print(*result)